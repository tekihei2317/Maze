// プレイヤーを扱うクラス
class Player {
  constructor(x, y, direction) {
    this.x = x;
    this.y = y;
    this.direction = direction;
  }
  /**
   * 矢印を描画する
   * @param {CanvasRenderingContext2D} context - 描画用のコンテキスト
   */
  drawArrow(context) {

  }
}